export const createRoom = async (req, res) => {
  // Assign roles, initialize room session
};

export const joinRoom = async (req, res) => {
  // Add user to room
};

export const endSession = async (req, res) => {
  // Finalize session, store feedback
};
